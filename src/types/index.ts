/**
 * Represents the intensity of the shadow applied to the screenshot frame.
 */
export type ShadowIntensity = "none" | "sm" | "lg" | "2xl";

/**
 * Represents the position of the custom logo overlay.
 */
export type LogoPosition = "top-left" | "top-right" | "bottom-left" | "bottom-right";

/**
 * Represents the style of the custom text overlay.
 */
export type TextStyle = "glass" | "solid" | "hidden";

/**
 * Represents the capture mode selected in the toolbar.
 */
export type CaptureMode = "Rectangle" | "Full screen";
